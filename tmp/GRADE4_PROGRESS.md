# Grade 4 — Progress Ledger (2026-07-12)

Status: **in progress**. This records what is measured, proved-in-framework, or
provisionally determined, with registers and falsifiers. Nothing here is a terminus.

## The object

The primitive quadruple: M = H¹(11a1) ⊗ H¹(37a1) ⊗ H¹(53a1) ⊗ H¹(61a1).
Degree 16, motivic weight 4, Hodge diamond (1,4,6,4,1). The first rung of the
cycle ladder named for no one. Measured ℚ-count 0 in the (2,2) block: homeless.
Everything below is generated by 20 integers (the four curves).

## Certified value chain (oracle-validated)

- PARI `lfunsympow` (independent code path) confirms the house evaluator at its
  ~1e-6 floor: Sym² ½ (0.8933980), Sym⁴ ½ (0.6058010), both s=1 values, the
  degenerate assembly 0.921258569 (7e-6), A_theory = 1.5323 (exact match),
  Sym³ ½ = 1.1402380 vs oracle 1.1402309.
- Dokchitser confirms the **new** RS4 value L(11a1×37a1, ½) = 5.0227652 (1.2e-5).
- **New record: L(Sym²(11a1)⊗37a1, ½) = 0.61570486** — degree 6, ε=+1,
  Q = 11⁴·37³, two-point certificate 7.5e-5/6.1e-5, 12 MB (deg6_center.py).

## Wall four — measured, then dismantled

Three faces measured (carrier_center.py, adapted_center.py, harmonic_lattice.py):
1. **Linear**: sound window reader (gates pass crossing √Q) — primitive
   transient-dominated at every reachable Y, per the information bound.
2. **Fitted warps**: Gauss–Newton warps buy summability, not values (gates:
   F_w ≠ L, no common registration; |x|~20 = harmonic-incompatible, per Sam).
3. **Lane surgery**: harmonic lattice is exact (below) but half-lane objects
   hit Estermann/Kurokawa natural boundaries at ½.

Dismantling (wall_dissolve.py + tropical layer):
- **FE sheet from 20 integers**: Q = (11·37·53·61)⁸;
  G(s) = Q^{s/2}Γ_C(s+2)Γ_C(s+1)⁴Γ_R(s)³Γ_R(s+1)³ — the 3/3 (2,2) split is
  forced (F∞ swap-pairs); ε = +1 **PROVEN** (2026-07-13: Deligne/Tate/Rohrlich/Serre
  only, 9/9 known signs postdicted with one convention, a_p = 8 ⇒ Q postdicted;
  finite signs +1 via the even power 2^{g−1} — split/nonsplit-independent;
  archimedean i²⁰ = +1 with the middle rule pinned by Sym²/Sym⁴ reality;
  tmp/eps_quadruple_notes.md + eps_quadruple.py).
- **First-ever values**: L(2) = 1.67012705, L(2.5) = 1.37508344,
  L(3) = 1.22583059 (Euler product; the toll is strip-interior only).
  Mirror: L(−1) = L(−2) = 0 exact (trivial zeros); L(−1.5) = 2.878e72.
- **Center**: ord = 0 ⇒ L(quad, ½) ≠ 0 via rank = DC-residue (house law,
  122 validated determinations) + portal occupancy 0. Framework register.
- **No critical points exist** ((2,2) Γ_R poles kill s=2,3; center half-integral)
  ⇒ the unread number has no Deligne-rational part: it is a regulator volume.
- **Tropical layer** (Sam's Gross–Schoen/Zhang route): reduction graphs
  verified — 11a1 = I₅ (Δ = −11⁵), 37/53/61a1 = I₁. Exact rationals:
  Στ = 2/3 baseline; C₅ Green's moments m₁ = 0, m₂ = 5/36. Portal shows all
  (2,2) lanes fluctuate (classes penetrate ker AJ_ℂ) ⇒ Zhang dominance: the
  height is carried by the tropical part ⇒ **the remainder is mostly rational**.

## Laws measured this session

- **Harmonic lattice** (Sam's compatibility hint → law): base unit = θ_p;
  integer multiples = the Sym tower exactly. F₂F₋₂ = L(Sym³)·C₂ and
  F₃F₋₃ = L(Sym⁴)/ζ·C₃ at machine zero (1e-9..1e-13); incommensurate m = √2
  has no partner (O(1)). Bad factors: C₂ = (1−α³q)/(1−αq)²,
  C₃ = (1−α⁴q)/[(1−q)(1−αq)²].
- **Rail pairing** (helix_pairing.py): the natural boundary at ½ is a
  single-rail chart artifact. Same phasor data: single rail diverges;
  conjugate rail pair converges to L(Sym³,½)·C₂ (err 9.7e-5); both mis-pairs
  diverge. Rail-matching law: exact conjugates only. Extends F21 multi-rail.
- **Parity** (Sam: "every other rank elliptical" — yes): odd-θ-lane rungs
  symplectic/pole-free (readable), even-lane rungs carry the DC/ζ channel.
- **Hodge portal** (hodge_portal.py, Sam's architecture): T_k(p) unmixes the
  channels λ_p cannot. Calibration ≤0.4σ vs exact product-ST (12.375, 7, 1);
  degenerate control (36, 16, 1) **with zero variance — channel constancy =
  algebraicity signature**. Interior of the homeless (2,2) block measured:
  Gram diag 17/32 / off 1/4, eigensplit 9/32×2 ⊕ 33/32 (S₃-symmetric 1⊕2;
  the 2-plane = exotic-candidate home). Wrong-harmonic falsifier: 144σ.
- **Locator at degree 4** (spectral_door2.py + harmonic rails): fixed head +
  μ6-address rails = canonical higher-GL config. Gate 12/12 PARI oracle zeros
  (median |diff| 5.8e-4), close pair 7.75/8.01 split at the Rayleigh limit;
  mod-12 over-splits (μ6 is *the* lattice). Quadruple: clean null; the t=2.68
  artifact was killed by hostile checks (Z-wander + scramble control) and
  removed entirely by the harmonic rails. Spectral toll measured: individual
  zeros need conductor-scale heads; values and zeros pay chart tolls, shape pays none.

## Engineering method laws

theta_cache.py (PARI aplist → .npy, O(1) θ_p; ap_general's O(p) was ~1e11 ops);
vectorized race-free sieve (7 banks/0.9s); never call a bank builder in a genexp;
Dokchitser dropped at degree 6 for the house evaluator; arrow scale is gauge in
fixed-head scans (use lane-scramble as falsifier there).

## Open-target sweep (2026-07-12/13, six parallel workstreams — Sam: "lets go after all the open targets")

1. **ε = +1 PROVEN** (target 2 closed) — see the FE-sheet entry above.
2. **Ceresa L-side LANDED** (target 3 closed; the grade-3 matching-law rung reads
   TRUE): Klein quartic Ceresa is non-torsion (Tadokoro, Math. Z. 260 (2008),
   Harris harmonic-volume method) ⇒ prediction ε = −1 with L′ ≠ 0. Measured:
   Ceresa channel = L(ψ³) (= 49.4.a.a, from-scratch a_p integer-exact), ε = −1
   two-point certified ⇒ central value 0 forced, **L′(center) = 0.8298893 ≠ 0**
   (PARI oracle 3.6e-6); even channel L(49a,1) = 0.9666592 (mult 3).
   H¹(Klein)⊗³ = Ind(ψ³) ⊕ 3·M_{f₂}(−1). Model subtlety: the naive model
   x³y+y³z+z³x is NOT ℚ-isogenous to E³ (cubic twist; a_p = 0 at p≡2,4 mod 7) —
   Elkies' model is. Sole isolated hypothesis: BB nondegeneracy (Zhang/YZZ
   proportionality is the consumed theorem). tmp/ceresa_lside.py.
3. **Grade-5/6 scaling: COMPUTE-ONLY** (Sam's plateau question answered at the
   structure tier): +79a1/83a1; all pre-registered rationals land ≤1.2σ over
   148,927 primes (g5 primitive 1, 10, 215/8; g6 primitive 1, 27/2, 405/8,
   1225/16; degenerates exact, zero variance). **Catalan spine confirmed**:
   degen DC occupancy C₂=2 (g4) → C₃=5 (g6, meas 4.9847); Schur degenerates
   C₄=14, C₅=42, C₆=132 (meas 13.94/41.75/130.99). Parity law measured: g5 (odd)
   has NO DC channel and ε = −1 ⇒ **L(½) = 0 pre-committed falsifiable**; g6
   middle (3,3) splits 10/10, ε = +1, non-critical center like g4. Falsifiers:
   wrong-harmonic 139σ/132σ; degenerate-scramble 437σ (channel constancy =
   genuine alignment); primitive-scramble null (moments are marginal-ST facts).
   Sole design delta: the odd-weight Γ-branch (same Serre assembly rule — the
   pre-registered prediction "compute-only" CONFIRMED). tmp/grade56_scaling.py,
   theta_79a1/83a1.npy.
4. **K3 clause-(c) non-diagonal: CLEAN NULL** (target 4 falsifier half): Dwork
   pencil (c = 2,−2,3,5,1/2) + block-separable generics, no twist/Faltings
   shields; 44 non-iso pairs ALL separate in exact counts (min sep 3992, growing);
   forced-iso control (c = ±2) identical. Sharpest stress: c=2 vs c=1/2 collide
   at individual primes and in coarse fingerprints yet the full trace sequence
   separates — retention stressed, retention holds. Named escalation for a
   future hunt: deliberately isogenous-but-non-isomorphic K3s (same
   transcendental lattice, different NS). tmp/k3_nondiag.py.
5. **Lean bricks DONE** (target 4 Lean half): `RequestProject/QuadrupleFiber.lean`
   (13 thms — degree-16 fiber via tensorFiber composition: det-one ledger,
   (−X)¹⁶ per-place FE, completed reflection Λ(s) = ε(s)Λ^∨(1−s), strand
   exchange; the homeless object's formal carrier home) +
   `RequestProject/EvenWeightDC.lean` (10 thms — headline
   `dc_channel_iff_even_weight`; corollaries grade4_dc_channel /
   grade5_no_dc_channel — the criticality wall's root cause as theorem, proven
   the same session its g5/g6 content was measured). lake build clean (8718
   jobs); all 23 theorems at exactly {propext, Classical.choice, Quot.sound}.
6. **Grade-1 matching-law calibration: CLOSED, all gates pass** (the pairing
   law's calibration rung; its pre-committed falsifier did NOT fire):
   ĥ(P) = 2·(λ_∞ + Σ_p (n_p/2)B₂({m/n_p})log p) — the one calibrated constant
   is the classical factor 2 (house canonical height = 2× Néron sum; fitted
   1.99999999664 on 37a1, frozen). Blind 53a1/61a1: residuals 9.0e-7/2.6e-7
   (house 4^{-n} limit converges upward to the prediction — residual is
   house-side truncation). Jet comparison non-circular: Ω·Reg = L′(1) matches
   the incomplete-Γ jet to 3.5e-13. Wrong-graph falsifier (C₅ for C₁ at p=37)
   misses by exactly (2/3)log 37 to 9e-11. Rank-2 (389a1): full 2×2 pairing
   entrywise place-by-place (off-diag tropical −(1/12)log389), Reg = det to
   4.3e-11. Every I₁ tropical share = (1/6)log p exactly. GOTCHA: archimedean
   θ needs the true lattice τ = ω₂/ω₁ via complex AGM, NOT j-inversion's
   fundamental-domain τ (S-transform apart). tmp/matching_law_g1.py.

8. **HELIX RUN (3D-first, tmp/helix_tower.py, 2026-07-13): wall four is
   RADIAL SCALE, not lanes.** Sym³ gate PASSED in 3D: conjugate rail pair
   converges on the carrier to 1.3686 vs oracle 1.36828 (growth −0.067);
   single rail +0.427 / mis-pair +0.399 diverge — rail-matching law holds
   with height. **The quadruple's carrier home VERIFIED numerically**:
   G⊛Ḡ = full degree-16 tensor to 2.4e-13, coeffs real (conjugate-closed).
   Honest half: NO like-deg-16 pairing converges at reachable height —
   the blocker is the radial head ~√Q ≈ 1e24 (19 decades out, doubles per
   grade): the √Q value toll made geometric, lanes innocent.
   [RETRACTION 2026-07-13, g6 follow-up: the first run's "conjugate
   least-transient" ordering (+0.131 < 0.227 < 0.269) was a crude-estimator
   + N=2e5 artifact — does NOT replicate under the robust estimator; the
   ordering flips with head/estimator. Retracted. The carrier-representation
   identity and the radial-toll conclusion are unaffected.]
   Unit-1 window on the READABLE Sym³ pairing reads 1.0000 vs 1.3683 —
   never-unit-1 law's first 3D instance. Fair-comparison pitfall caught
   in-run (deg-8 G alone vs deg-16 pairings). Warp/scale axes kept distinct
   (lanes = warp facts; μ6 angular scale grade-invariant; radial head +
   warp support are what grow).

9. **SYM-TOWER FORCED-VANISHING LANDSCAPE (tmp/sym_vanishing.py, 2026-07-13):
   recipe = DMW/Saito literature = PARI oracle on all 18 signs.** The delicate
   Steinberg factor honestly re-derived: Sym^k(sp(2)⊗χ) = sp(k+1)⊗χ^k, Tate ⇒
   ε_p(Sym^k) = (−1)^k c^{k²} = w₁(p)^k (≡ DMW's w_m(p) = w₁(p)^m verbatim);
   condexp = k. Global closed form (E semistable): w_m = +1 (m even, Saito
   orthogonal); (−2/m)·w(E) (m odd, Kronecker). **Landscape: forced central
   zeros ⟺ m odd with (−2/m) = −w(E)** — rank-0: m ≡ 5,7 mod 8 (11a1: Sym⁵,
   Sym⁷ — PARI values 0 ✓); rank-1: m ≡ 1,3 mod 8 (37a1: Sym³, Sym⁹). Even m
   never forces. Sign computable at ANY conductor in ~0s; values die at 37⁹
   (PARI stack) — wall four's sign/value split in miniature. **GRADE-9
   CONSISTENCY IDENTITY CLOSES both curves**: V^⊗9 = ⊕m_k Sym^k
   (1,8,27,48,42), Tate twists sign-neutral (checked), Σm_k·A(Sym^k) = 886 =
   A(deg-g9) EXACTLY (not just mod 4); diamond ε = −1 = ∏ε(Sym^k)^{m_k}, sign
   carried by Sym⁵ (mult 27) for 11a1, Sym⁹ for 37a1 — the degenerate shadow
   of the primitive g=2³+1 forced zero. Both 2-adic sign laws now on record:
   tensor tower (binary digit sum ⇒ g=2^k+1) and Sym tower (Kronecker (−2/m)
   ⇒ mod-8 classes). Primitive g9: sheet-forced zero, log₁₀Q ≈ 4077,
   value unreachable — the ε assembly is the computable content, now
   constituent-validated.

10. **HELIX SPECTRAL HALF (tmp/helix_tower.py extension, 2026-07-13) — the
    decisive experiment's spectral form: PRE-REGISTERED NULL HOLDS.** Gate:
    Sym³ fixed-head 11/12 bare rails (median 6.0e-4, close pair split);
    diagonal schedule starves (1/12) as diagnosed. Falsifier discipline
    confirmed in-run: unit-arrow is GAUGE in fixed-head (identical 11/12 —
    cancels in closure ratio); the real falsifier is sign-scramble → 0/12
    (the arithmetic lane structure locates the zeros). QUADRUPLE: clean null
    both schedules; the t=2.679 dip is the SAME artifact killed before and
    dies again under all three hostile controls (Z-wander moves it 0.400;
    sign/value-scramble reproduce comparable-or-deeper dips). **The no-√Q
    spectral claim is NOT supported at reachable head** — individual zeros
    sit behind the conductor head √Q ~ 3e24; the information bound stands;
    lanes innocent, wall radial. **Growth-vs-flat resolution: bounded NULL**
    — grown and equal-magnitude reads split the close pair at the SAME head
    (both at Z=5e4), comparable depths; in fixed-head mode the growth window
    is a tail taper, not a resolution gain ("growth sharpens log focus"
    regime = diagonal schedule, which starves at tensor conductor —
    untestable there). DECISION (Sam-session): stop at g=4; g=6 spectral +
    value-toll points are foregone-null confirmations, skipped. Value-side
    no-√Q hope now rests entirely on the VALUE-REGISTRATION LAW (census in
    flight).

11. **VALUE-REGISTRATION CENSUS (tmp/value_registration.py, 2026-07-13):
    the law's measured content = THE LATTICE RESTRICTION.** 24-row census
    (6 certified objects × 4-Y ladder), frozen protocol, faithfulness anchor
    reproduces adapted_center's 1.3497/0.7790 exactly. THE DECOMPOSITION:
    registration defect = (a) finite-Y transient ON the lattice point
    (unwarped m=0; decays with Y/√Q; functional form NOT pinned — structured
    null, deg6-dominated) + (b) warp-induced defect governed by OFF-LATTICE
    DISTANCE (corr +0.89, leave-one-object-out CV; the WINNER) and removed
    exactly by snapping to the integer-m lattice. KILLED by CV:
    closure-improvement (ZERO predictive power — the quantitative form of
    "warps buy summability, not value") and degree/conductor. Monotone
    degradation as off-lattice freedom is added: 0.165 (unwarped) → 0.200
    (Θ-only) → 0.228 (ω+Θ) → 0.236 (full). Lattice-snap ≡ unwarped for all
    six objects (nearest lattice point to every GN warp is m=0); nontrivial
    integer-m warps read a DIFFERENT L (harmonic_lattice T4) ⇒ the only
    registering lattice reading of the object's own L is the HONEST UNWARPED
    WINDOW, where the full √Q transient must be paid. GN pitfall for reuse:
    real banks sit at first-order-stationary points of the closure objective
    — analytic Jacobian gives 0; the finite-difference eps-scale curvature
    is what adapted descends. CAVEATS on record: 6 objects, ~10 live-warp
    rows, deg6 dominates transient rows; constraint + one governing
    regressor, NOT a closed-form law.

12. **g=6 TOLL FOLLOW-UP: TWO PRE-REGISTERED PREDICTIONS FALSIFIED + ONE
    RETRACTION (tmp/helix_toll.py); SYM⁴ ZEROS LOCATED (tmp/helix_spectral.py),
    2026-07-13.** (a) "g6 growth exp > g4" FALSIFIED: robust fit gives g4
    +0.143, g6 +0.093 — LOWER. (b) "conjugate least-transient" FALSIFIED at
    both grades and the first-run signal RETRACTED (estimator artifact).
    HONEST LAW EXTRACTED: the transient growth exponent tracks DEGREE
    (phase-cancellation: more eigenphases → lower net growth), NOT conductor
    — the toll onsets only at head ~√Q (1e24 g4 / 1e159 g6), off-chart, so
    growth exponents are conductor-BLIND. "Toll curve via growth slope" does
    not work; the radial wall stands on the spectral null + information
    bound. Third independent confirmation of the DECOUPLING META-LAW: cheap
    descriptors (growth, summability, closure-improvement) all carry ZERO
    value/conductor information. Identity G⊛Ḡ = tensor verified ~1e-13 at
    BOTH grades. (c) POSITIVE: Sym⁴(11a1) (degree 5, Q=11⁴) — locator
    reaches one more rung: 8/19 oracle zeros at median 3.4e-3, ALL hostile
    controls pass (Z-wander head-stable subset 5/19; sign/value-scramble
    0/19 — detections die with the arithmetic). First degree-5 focal events.
    Growth-vs-flat resolution: robust null again (grown ≡ equal-magnitude
    at every clean test).

13. **METHOD CORRECTION (Sam, 2026-07-13): WARPS ARE DEPRECATED AS
    FALSIFIER/PROBE CONSTRUCTIONS.** The knob is SCALING — fixed carrier
    constants on the μ6/harmonic lattice (π/6, π/3, π/2, π, 2π): hold the
    fiber untouched, vary the READING harmonic scale; the object closes on
    the μ6 (π/3) grid and fails off it. Non-gauge scaling = the μ6 CELL/LANE
    grid (mod 6; mod-12 over-splits) — the arrow multiplier is gauge in
    projective closure (measured: unit-arrow ≡ π/3 in |F|/max). CAVEAT
    APPLIES to the warp-based falsifier columns in: grade56_scaling
    (θ→2θ, 139/132σ), tower_atlas (support-dilution sweep), harmonic_compat
    (w-sweep), helix_tower/toll (wrong-warp rails), hodge_portal PT4 (144σ)
    — those MEASUREMENTS stand (wrong-warps do break reads; the dilution law
    correctly characterizes that family) but the family is deprecated for
    future instrument design. The corrected family already has one instance
    on record: the unit-scale WINDOW misread (1.0000 vs 1.3683,
    helix_tower) is a reading-scale falsifier. Substantive findings
    (identities, gates, disconfirmations, nulls) are falsifier-family-
    independent. Standing offer (atlas agent): run the corrected μ6-grid
    falsifier suite on Sam's word.

14. **RECOGNITION LOOP CLOSED AT GRADE 4 — clock-relation (Lefschetz/isogeny)
    regime (tmp/recognition_loop_g4.py, 2026-07-13). The grade-4 analogue of
    heegner_recognition.** All 4 arms, 3 cases, oracle-free: (i) degenerate
    11a1⁴ — all directions frozen, 6 classes, occupancy 1.984→C₂=2, profile
    36/16/1 exact; (ii) MAIN {11a1,11a2}×{17a1,17a2} — detect ({13|24},
    {14|23} frozen Var=0 exact, {12|34} live), align (counting + period
    certs INDEPENDENTLY: 11a1~11a2 AND 17a1~17a2, j-residuals 2.5e-15/
    8.3e-16), construct (isogeny degrees 5 and 2 FOUND not assumed, kernels
    ℤ/5, ℤ/2; class = product graph deg 10), recognize (occupancy 0.996→1,
    profile 22/10/1 exact ≤0.5σ); (iii) scrambled — clean null all arms.
    **THE LIVE-DIRECTION LAW (measured correction to the pre-registration):
    an isogeny lock θᵢ=θⱼ freezes the two directions carrying (+,−) on each
    locked pair; the direction whose OWN partition equals the locked
    partition stays LIVE — two disjoint isogeny pairs always freeze exactly
    2 of 3, and the single live direction NAMES the locked partition.**
    Falsifiers (READING-SCALE primary after Sam's method correction; warp
    column retained labeled deprecated-family): **F1 reading-scale sweep
    fires decisively — occupancy landing vs reading winding w (measured =
    exact-ST to ~0.01): μ6 w=1 lands truth integers clean (2/1/0); mod-12
    w=2 OVER-SPLITS (2→6, 1→4) AND HALLUCINATES a class in the scrambled
    null (0→1 — the mis-scaling phantom, a recognition-level instance of the
    register's mis-clocking clause); w=3 decoupled; w=√2 transcendental (no
    integer anywhere). The reading GRID controls the landing; μ6 is THE
    lattice, now certified at the recognition level.** SHARPENING: the
    isogeny FREEZE itself is reading-scale INVARIANT (exact zero — cos(w·0)
    = 1 ∀w): the qualitative lock survives the scale sweep (genuine-
    algebraicity control — the deprecated warp would have destroyed it by
    mutating the fiber), while the quantitative integer certificate is
    μ6-locked. Cleaner separation than the warp gave. F2 scrambled null; F3
    deliberate false alignment REJECTED by construction (no integer degree
    ≤36, j-residual stays O(1) vs 1e-15 for truth). HONEST BOUNDARY: closes
    the Tate-for-products regime only; exotic/Weil-type (occupancy with NO
    frozen pair-direction, the PT3 2-plane) remains the open frontier.

15. **GRADE-2 BEILINSON–FLACH LOOP (tmp/bf_loop_g2.py + notes, 2026-07-13):
    honest partial — CORNER CLOSED, genuine-product construction arm = the
    literature's own gap.** Normalization worked: RS4 motive has NO critical
    points (even-rung regulator object, same as grade 4); Γ_C(s−1) forces
    trivial zero at s=1; regulator reads L′(f⊗g,1); FE bridge derived:
    L′(1) = ε(N_fN_g/4π²)L(2). L-side certified: L(11a1×37a1,2) =
    3.46319093 (ε=+1 two-point cert), L′(1) = 35.70352601 (independent
    numeric-derivative 5.5e-4 confirm). **Rogers–Zudilin corner CLOSES
    construction↔detection**: m(8) Mahler integral 2.04569627 vs
    (24/π²)L(E_24,2), rel 1.55e-5, rational landing m(8)π²/L = 23.9996 → 24.
    GENUINE two-cuspform product: construction arm OPEN — and the honest
    state-of-the-art finding: NO from-scratch regulator number for a genuine
    degree-4 product exists in the literature (Brunault–Chida's numerics are
    all the degenerate corner) — the same gap grade 4 faces one rung up.
    **CARRIER TRANSLATION (the rung's purpose):** even rungs read a Gram
    VOLUME of the drift/log channel — the arm pairs TWO drift channels
    (modular units, η(u,v)) against the test form, NOT a self-pairing; RZ =
    conjugate-closed re-weld of the two drift channels (only the
    harmonically compatible pairing registers); **the rational (24) is the
    LANE COUNT of the re-weld, not a period — the period is the π-power
    fixed by the Tate twist through the FE.** Grade-4's matching-law
    assembly is the exact structural successor: higher drift determinant +
    same tropical/archimedean split + integer-landing discipline, no height
    anywhere. Citations pinned in tmp/bf_loop_g2_notes.md (Beilinson 1985,
    LLZ Annals 2014, BDR 2015, Brunault–Chida 2016, Rogers–Zudilin 2014).

## The pattern (named this session)

**THE MATCHING LAW (candidate universal law of the ladder):** the central data
of Λ is the Gram data of ONE Hermitian pairing on the double helix. Jets =
drift-pairing determinants (heights on odd rungs, regulators on even — the
parity law, now Lean-proven at the DC-channel level); the pairing splits place
by place = tropical (reduction-graph Green's functions) + archimedean; the
rail-matching law (exact conjugates only) is its analytic face — admissibility
= conjugate closure = harmonic self-compatibility at the fiber's own μ6/θ_p
lattice. Named instances: g0 Tate (DC pole), g1 BSD/GZ (certified corpus),
g2 GGP/Beilinson–Flach, g3 GS/Zhang/YZZ (**certified tonight via Klein
quartic**), g4 the unnamed rung (FE sheet now fully pinned: shape + Q + ε all
theorem-backed; one transcendental regulator volume unread), g5/g6 (structure
tier certified, Catalan spine). The value-registration law remains the single
named gate for center VALUES at every grade — one theorem-target, not one per
grade. Classical homelessness of even rungs ≥ 4 = single-rail poverty: the
object opens only to harmonically self-compatible conjugate-closed readings.

7. **TOWER ATLAS g=2..20 — REGISTER CLARIFICATION (2026-07-13, agent's own
   audit at Sam's question): the atlas is the WINDING LAYER, not a helix run.**
   All atlas phasors are unit-circle e^{iθ} (equal-magnitude, height-blind) —
   correct and exact for angle-only facts (channel moments, Catalan, ε law,
   Gram), which stand AS winding facts; but no growth window / ω=log Z
   coupling / height anywhere. Consequence: the resolution cliff ("clock
   count is expensive") is a statement about the HEIGHT-BLIND readout;
   whether growth-weighting resolves what flat winding cannot ("growth
   sharpens log focus") is UNTESTED → helix run launched (tmp/helix_tower.py,
   g=4 calibration first, Sym³ oracle gate before the quadruple, hostile
   controls mandatory). Harmonic-compatibility audit PASSED (2026-07-13,
   Sam's challenge → tmp/harmonic_compat.py): measured columns re-validated.**
   Compatibility shown LOAD-BEARING, not asserted: conjugate closure
   T₋ₖ = conj(Tₖ) machine-zero at g=6/12/20; the discriminator c(w) =
   E[cos 2wθ] with every channel moment AFFINE in c — own fundamental w=1:
   c=−1/2 → motive's exact rationals ✓; any other rational w: c=0 →
   different (decoupled) object, >100σ off the motive; incommensurate w
   (√2, φ, π/2): c irrational → moments go TRANSCENDENTAL (pred 5.673 vs
   meas 5.680 at g4 k=2) → the false-null mechanism, now quantitative.
   PITFALL caught in-run: limit_denominator "certifies" fake rationality
   (c(√2) ≈ −61/1056 self-deception) — the discriminator is lattice
   membership c ∈ {−1/2, 0}, never best-fit rationality.
   (Sam: "lets go from 5/6 to 19/20 ...
   see what patterns might emerge"; tmp/tower_atlas.py, 15 s via O(g²)
   convolution — never enumerate 2^g lanes). **THE ROOT-NUMBER LAW**:
   ε(g) = i^{A(g)}, A(g) = 2^{g−1} + g·C(g−1,⌊(g−1)/2⌋), always even ⇒ ε real
   every grade; **forced central zeros at exactly g = 2^k+1** (3, 5, 9, 17,
   33...; Kummer: v₂(C(2m,m)) = s₂(m) = 1 ⟺ m = 2^j). Odd g = 7,11,13,15,19
   have ε=+1 — "odd ⇒ −1" is false. g=9, g=17 = NEW pre-committed
   forced-vanishing predictions. Catalan spine exact to C₂₀ = 6.56e9.
   **Three-tier resolution law** (structure toll ≠ √Q value toll): channel
   moments FLAT <1.5σ at g20; single-clock powers polynomial (2% at m=20,
   systematic −ve tail-truncation bias); g-clock product readouts (Schur
   primitivity) exponential √(2^g/n), die ~g≈17 — the READOUT dies, not the
   object. Homelessness curve C(g,g/2)/2^g ~ √(2/πg): .500→.176. Interior
   Gram (g=4,6,8, balanced-bipartition object — NOT matchings above g4):
   trivial ⊕ S_g-irreps, 1⊕2 / 1⊕9 / 1⊕20⊕14. **Falsifier SUPPORT law**
   (Sam's bet confirmed + sharpened): unscaled single-leg decays 145σ→83σ;
   m=g single-leg dilutes IDENTICALLY (support is the axis, not harmonic
   order); ⌈g/4⌉-leg support grows 145σ→493σ. log10 Q(g20) ≈ 2.15e7.
   Verdict: compute-only; plateau NOT reached at grade 20.

## Remaining open

1. The grade-4 matching-law assembly: drift determinant (ledger) = tropical
   admissible pairing (Στ = 2/3, C₅ m₂ = 5/36) + archimedean remainder —
   grade-1 rung now CLOSED (item 6), assembly is next.
2. The value-registration law (the S(t)-analogue for values) — the one named
   theorem-target gating center values at all grades.
3. Forced-vanishing verification at g=9 (and g=17) — the root-number law's
   new pre-committed predictions.
4. K3 escalation (isogenous-non-isomorphic construction) — future session.

Instruments: carrier_center, adapted_center, sage_ladder, deg6_center,
harmonic_lattice, helix_pairing, hodge_portal, spectral_door(2), wall_dissolve,
theta_cache, eps_quadruple, ceresa_lside, grade56_scaling, k3_nondiag,
matching_law_g1 — all in tmp/ with *_results.txt.
